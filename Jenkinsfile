pipeline {
    agent any
    
    stages {
        stage("stage 1") {
            steps {
                echo "calling from main branch"
                echo "process finished at ${date}"
            }
        }
    }
}