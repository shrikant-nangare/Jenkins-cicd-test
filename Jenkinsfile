pipeline {
    agent any
    
    stages {
        stage("stage 1") {
            steps {
                echo "calling from feature1 feature2"
                echo "process finished at ${new Date()}"
            }
        }
    }
}
