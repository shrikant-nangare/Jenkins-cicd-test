pipeline {
    agent any
    
    stages {
        stage("stage 1") {
            steps {
                echo "calling from feature1 branch"
                echo "process finished at ${new Date()}"
            }
        }
    }
}
